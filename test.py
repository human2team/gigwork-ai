# # ======================================================
# # 데이터베이스 연결 및 테스트용 테이블 목록 출력
# # ======================================================

# from app.db.database import PostgresDB

# db = PostgresDB()
# db.test()
# db.close()

# # ======================================================
# # 임베딩 생성 테스트
# # ======================================================

# from app.vector.embedder import Embedder

# embedder = Embedder()
# text = "밝고 긍정적인 마인드의 소유자"
# vec = embedder.create_embedding(text)

# print(f"🧩 입력 문장: {text}")
# print(f"✅ 벡터 길이: {len(vec)}")
# print(f"🔹 앞부분 샘플: {vec[:5]}")

# # ======================================================
# # LLM 채팅 테스트
# # ======================================================

# from app.llm.openai_client import LLMClient

# llm = LLMClient()
# messages = [
#     {"role": "system", "content": "너는 알바 추천 도우미야. 사용자가 원하는 알바를 추천하기 위해 정보를 수집해야 해. 원하는 지역이나 업종 등을 물어봐."},
#     {"role": "user", "content": "안녕?"}
# ]
# reply = llm.chat(messages)
# print("💬 모델 응답:", reply)

